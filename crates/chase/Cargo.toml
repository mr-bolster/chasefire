[package]
name = "chase"
description = "Decides which decoded frames to believe: parity, continuity and freewheel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
# El motor va permisivo a proposito: las aplicaciones que lo usan pueden
# ser de otro color, y las mejoras al propio motor siguen abiertas.
license = "MPL-2.0"
repository.workspace = true
authors.workspace = true

[dependencies]
ltc = { path = "../ltc" }

[dev-dependencies]
cue = { path = "../cue" }
