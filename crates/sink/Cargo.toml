[package]
name = "sink"
description = "Outputs for fired cues: OSC today, MIDI and RTP-MIDI next"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
# El motor va permisivo a proposito: las aplicaciones que lo usan pueden
# ser de otro color, y las mejoras al propio motor siguen abiertas.
license = "MPL-2.0"
repository.workspace = true
authors.workspace = true

[dependencies]
cue = { path = "../cue" }
midir = "0.10"
rtpmidi = { path = "../rtpmidi" }
ltc = { path = "../ltc" }
